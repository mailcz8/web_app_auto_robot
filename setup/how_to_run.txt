Setup virtualenv:
pip install virtualenv
virtualenv venv
source venv/bin/activate
deactivate

Export driver:
cd <your_driver_parent_path>
pwd
export PATH=$PATH:/<your_driver_parent_path>/drivers/

Install Libraries:
pip install -r requirements.txt

Run testcases
robot --argumentfile config/args.arg  -d result tests/
